import { PageHero, SiteFrame } from "../components";
import { application, faqItems } from "../content/join";
import { sitePath } from "../site-path";

export default function Join(){return <SiteFrame><PageHero index="06" eyebrow="Join us" title="Join us" intro="Graduate student and undergraduate research internship information."/><section className="admission-status shell" aria-labelledby="admission-status-heading"><h2 id="admission-status-heading">모집 안내</h2><ul>{application.statusNotices.map((notice)=><li key={notice}>{notice}</li>)}</ul></section><section className="application-section shell"><h2>Application</h2><div className="application-content"><h3>Graduate students &amp; undergraduate research interns</h3><p>{application.recruiting}</p><p>{application.instructions}</p><div className="application-email"><span>Email</span><a href={`mailto:${application.email}`}>{application.email}<b aria-hidden="true">↗</b></a></div></div></section><section className="faq shell"><h2>FAQ</h2><div>{faqItems.map((item,index)=><details key={item.id}><summary><span>{String(index+1).padStart(2,"0")}</span>{item.question}<b>+</b></summary><div className="faq-answer">{item.content.map((block,blockIndex)=>block.type === "paragraph" ? <p key={blockIndex}>{block.text}</p> : <ol key={blockIndex}>{block.items.map((entry)=><li key={entry}>{entry}</li>)}</ol>)}{item.link && <a href={item.link.href.startsWith("/") ? sitePath(item.link.href) : item.link.href} target={item.link.href.startsWith("/") ? undefined : "_blank"} rel={item.link.href.startsWith("/") ? undefined : "noreferrer"}>{item.link.label} <span aria-hidden="true">↗</span></a>}</div></details>)}</div></section></SiteFrame>}
